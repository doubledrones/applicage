PortSystem          1.0

name                Things
version             1.5a
categories          office
platforms           darwin
maintainers         pr0d1r2
description         Things.app is a productivity application for GTD system
homepage            http://culturedcode.com/things/
master_sites        http://culturedcode.cachefly.net/things/
distname            Things_${version}

checksums           md5     cbf90ee96aab51a817053db157d0f0f3 \
                    sha1    d560f06de69eea257b8b42a6ad410c60ec3b021c \
                    rmd160  6b4af1a17b8584466edc4686e8294ae482573c3c

use_zip             yes
worksrcdir          ${name}

configure           {}
build               {}

pre-fetch {
    if {${os.platform} == "darwin" && ${os.major} < 9} {
        ui_error "${name} does only run on Mac OS X 10.5 or greater."
        return -code error "incompatible Mac OS X version"
    }
}

destroot {
    copy ${worksrcpath}.app ${destroot}${applications_dir}
}
