PortSystem                  1.0

name                        Evernote
version                     156801
platforms                   macosx
categories                  office
maintainers                 pr0d1r2
license                     freeware
supported_archs             i386

description                 Remember everything.
homepage                    http://evernote.com/
master_sites                http://evernote.s3.amazonaws.com/mac/release/

use_zip                     yes
distname                    ${name}_${version}

checksums           md5     e972cc5659c5b5b87a42287d3c04c91f \
                    sha1    b57acd0d193f6dd9c512d110b4d6557c162eafc9 \
                    rmd160  2ccac70a579d40db3a8de86a47d7285f64b81fab

use_configure               no

build {}

pre-fetch {
    if {${os.platform} == "darwin" && ${os.major} < 9} {
        ui_error "${name} does only run on Mac OS X 10.5 or greater."
        return -code error "incompatible Mac OS X version"
    }
}

destroot {
  copy ${workpath}/${name}.app ${destroot}${applications_dir}
}
